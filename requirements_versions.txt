# Core dependencies (pinned for stability)
torchsde==0.2.6
einops==0.8.0
transformers==4.42.4
safetensors==0.4.3
accelerate==0.32.1
pyyaml==6.0.1
pillow==10.4.0
scipy==1.14.0
tqdm==4.66.4
psutil==6.0.0
pytorch_lightning==2.3.3
omegaconf==2.3.0
# gradio 3.x is required — 4.x has breaking API changes
gradio==3.41.2
pygit2==1.15.1
# headless OpenCV to avoid Qt/display conflicts on Windows
opencv-contrib-python-headless==4.10.0.84
httpx==0.27.0
# onnxruntime-gpu for CUDA acceleration; falls back to CPU automatically
onnxruntime-gpu==1.18.1
timm==1.0.7
# numpy pinned — newer 2.x breaks many packages (onnxruntime, insightface, etc.)
numpy==1.26.4
tokenizers==0.19.1
packaging==24.1
rembg==2.0.57
groundingdino-py==0.4.0
segment_anything

# inswapper / insightface
protobuf>=4.23.2,<5.0
addict
future
lmdb
# opencv-python conflicts with opencv-contrib-python-headless — removed duplicate
requests
scikit-image
# tb-nightly removed: causes numpy/protobuf conflicts and is unnecessary at runtime
yapf
lpips
insightface>=0.7.3
facexlib==0.3.0

# photomaker
# diffusers pinned to 0.26.1 to match InstantID pipeline code
diffusers==0.26.1
# huggingface-hub pinned — newer versions break hf_hub_download API used in pm.py/instantid.py
huggingface-hub==0.23.2
# 'spaces' is HuggingFace Spaces only — removed, breaks local Windows installs
omegaconf
peft
safetensors

# instantid
controlnet_aux==0.0.7